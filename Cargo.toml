[package]
name = "city_builder"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18"

# Enable optimizations for dependencies in dev builds for reasonable performance
[profile.dev.package."*"]
opt-level = 2
