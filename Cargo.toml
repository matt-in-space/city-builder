[package]
name = "city_builder"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18", features = ["free_camera"] }
bevy_egui = "0.39"

# Enable optimizations for dependencies in dev builds for reasonable performance
[profile.dev.package."*"]
opt-level = 2
